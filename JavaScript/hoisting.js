// hoisting, just don't do it
{

    console.log(greeting);

    var greeting = "Hello, everyone"; // hoisting is a feature in js
    // declared variables are hoisted to the top of their scope
    // but the values are not


}

